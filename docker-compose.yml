services:
    moi-goroda-redis:
        container_name: moi-goroda-redis
        image: redis:8.8-alpine
        ports:
            - "${REDIS_PORT:-6379}:6379"
        volumes:
            - moi-goroda-redis:/data

volumes:
    moi-goroda-redis:
